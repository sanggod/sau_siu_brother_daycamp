// ── FILL THIS IN, then redeploy. ───────────────────────────────────────────
// Firebase console → Project settings → Your apps → SDK setup and configuration.
// You also need Realtime Database created (any region) and rules that allow
// read/write for the camp day, e.g.:
//   { "rules": { "flipgame": { ".read": true, ".write": true } } }
//
// Leave databaseURL empty and the game runs in LOCAL mode instead:
// all tabs/windows on ONE machine stay in sync, no network needed.

export default {
  apiKey: "",
  authDomain: "",
  databaseURL: "",          // e.g. https://your-project-default-rtdb.asia-southeast1.firebasedatabase.app
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
